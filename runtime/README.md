# Runtime Artifacts

This directory is intentionally kept out of source control except for lightweight placeholders.

Large generated artifacts such as projected maps, PCD files, OctoMap `.bt` files,
closed-loop run logs, and test records should be stored outside Git or copied in
from the robot/runtime environment when needed.
